import { SpotlightContentDirective } from './spotlight-content.directive';

describe('SpotlightContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SpotlightContentDirective();
    expect(directive).toBeTruthy();
  });
});
