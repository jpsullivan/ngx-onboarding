import { TargetDirective } from './target.directive';

describe('TargetDirective', () => {
  it('should create an instance', () => {
    const directive = new TargetDirective();
    expect(directive).toBeTruthy();
  });
});
